<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yws.dao.MsgIdDAO">
    <insert id="setnx"  parameterType="MsgId">
        insert into msgid select distinct #{id} from msgid
        where not exists (select id from msgid where id = #{id})
    </insert>

    <select id="exist" parameterType="MsgId" resultType="java.lang.Integer">
        select count(1) from msgid where id = #{id}
    </select>

</mapper>
